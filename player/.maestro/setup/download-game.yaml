appId: com.spela.player
---

# Helper flow: Download a game if not already downloaded.
# Called conditionally via: runFlow: when: visible: "Download"
# After this flow completes, the "Play" button will be visible.

- tapOn: "Download"

- extendedWaitUntil:
    visible:
      text: "Play"
    timeout: 8000
